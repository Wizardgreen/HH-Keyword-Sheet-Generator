import Box from '@mui/material/Box';

const {{pascalCase name}} = (props) => {
  return (
    <Box className="{{kebabCase name}}">
      {{pascalCase name}}
    </Box>
  );
};

export default {{pascalCase name}};